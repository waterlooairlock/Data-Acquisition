const express = require("express")
const router = express.Router()
const mongoose = require("mongoose")



router.get('/api/getcondition', (req, res)=> {

    //Returns the following details of the sensor:
    // Most recent value
    // Still works?
    // idk what else we could add


})

module.exports = router