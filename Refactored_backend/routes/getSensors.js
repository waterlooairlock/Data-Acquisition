const express = require("express")
const router = express.Router()
const mongoose = require("mongoose")



router.get('/api/getsensors', (req, res)=> {

    //Return most recent set of values for all sensors together


})

module.exports = router